spring.application.name=tk2me

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tk2me?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=farhan93933
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# SQL Initialization
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# JWT Configuration
app.jwt.secret=tk2meSecretKeyForJwtAuthenticationVerySecureAndLongKey
app.jwt.expiration=86400000

# Server Configuration
server.port=8080
